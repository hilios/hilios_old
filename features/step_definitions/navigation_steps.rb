Then /^should not have any broken link$/ do
  @visited_paths, @collected_path = ['/'], []
  do
    
  end while @visited_paths.length > 0
  pending # express the regexp above with the code you wish you had
end